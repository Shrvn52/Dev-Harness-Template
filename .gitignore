# ── Build output ────────────────────────────────────────────────────────────
node_modules/
dist/
*.tsbuildinfo
.vite/

# ── Local data / env ─────────────────────────────────────────────────────────
.env
.env.local
.env.*.local
# Any local SQLite file a dev points DB_PATH at, plus WAL/journal sidecars.
*.db
*.db-*

# ── Test / tooling artifacts ─────────────────────────────────────────────────
coverage/
playwright-report/
test-results/
.worktrees/
# docs-audit skill reports — machine-generated, dated, local by default.
# Commit one deliberately if you want the drift history in-repo.
audits/

# ── OS / editor / local tool caches ──────────────────────────────────────────
.DS_Store
*.log
.impeccable/

# ── Claude Code: machine-local, NEVER committed ──────────────────────────────
# The committed .claude/ harness (settings.json, hooks, agents, commands, skills)
# travels with the repo. These accrete per-machine and must stay local:
.claude/settings.local.json
.claude/worktrees/
.claude/launch.json
.claude/*.lock
.claude/workflows/
